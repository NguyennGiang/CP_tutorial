#include <bits/stdc++.h>

using namespace std;

const long double esp = 1e-6;

int main(){
	int t;
	cin >> t;
	while(t--){
		int w, h;
		cin >> w >> h;
		int a, b, c, d;
		cin >> a >> b >> c >> d;
		int n, m;
		cin >> n >> m;

		double need_x = -1;
		double need_y = -1;

		while(a < n && a < w && c < w){
			a++;
			c++;
		}

		while ()
	}
}